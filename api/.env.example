PORT=4000
HOST=0.0.0.0
# Optional: leave unset to use in-memory user store for local Expo Go smoke tests
# DATABASE_URL=postgres://findr:findr@localhost:5432/findr
JWT_SECRET=dev-only-change-me-findr-jwt-secret
JWT_EXPIRES_IN=7d
